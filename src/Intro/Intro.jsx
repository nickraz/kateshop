import React from 'react';
import css from './Intro.module.css';

const Intro = () => {
    return (
        <div class={`row ${css.intro}`} data-container="container">

        </div>
    );
}

export default Intro;